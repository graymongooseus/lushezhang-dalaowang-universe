# 资料入库标准 · v9

与 [数据库结构标准](数据库结构标准.md)、[工作流](工作流.md) 一起执行。新增视频、补充证据、纠正姓名、调整关系、回退资料均适用。旧版一次性 SQL 导入脚本不得直接在 v9 库上运行。

## 1. 必须先准备的材料

1. 完整阅读本期可取得的字幕/原稿；记录未取得或未核听的范围。
2. **先写原创 Markdown**：元信息、全部人名核对、每段人物细节、事件、主张、起止秒数、来源链接、矛盾、未确认称谓。保留全部相关细节，不只提取主线。
3. 给每个具名人物核对姓名、机构、年代、地点和同名身份。复用稳定 ID；真实公开笔名、明确确认的绰号可以作别名，字幕错字不入名录。确认不了的新身份只进 unresolved_mentions，不猜实名或照片。
4. 每条资料至少一条具体引用，尽量定位到秒数、页码或段落。视频只能直接证明主播如何说，不能自动证明其所述事件真实发生。
5. 分别填实际时间、报道时间、收录时间；未知留 NULL；强度、好感度、阵营归属没有依据不填。
6. 职业用现行 11 类字典；主标签必须在职业多标签中。头像色环只跟主标签。

## 2. 写入与审核

### 本地维护接口

使用 `scripts/data_standard.py` 的 `submit_batch()` 或命令行 `scripts/review.py submit`，不能直接 UPDATE 受管领域表。每个批次包括：

```json
{
  "reason": "补充本期有时间定位的资料",
  "idempotency_key": "video:视频ID:本次修改唯一编号",
  "changes": [{
    "table": "relationships",
    "entity_id": "既有稳定关系ID",
    "base_revision_id": "读取时的当前修订ID",
    "content": {"说明": "实际提交为该领域表全部字段，不包含 statement_id/revision_id"},
    "citations": [{"passage_id": "已登记来源片段ID", "stance": "supports", "summary": "本条引用支持的具体范围"}]
  }]
}
```

该示例展示封装格式，content 的占位文字不能直接提交。用 `snapshot(dict(db_row))` 取得完整当前记录，修改必要字段；`validate_content()` 拒绝漏字段、多余字段、日期错误和错误的人物最高履历引用。

- 新条目 `base_revision_id=null`，必须使用新稳定 ID；新人物和其主职业标签在同一个批次提交。
- 新引用先登记来源、版本、片段；新机构/图片先登记必要的元数据。现在这些登记是可信维护者的本地导入事务；来源 ID、路径、许可、时间码、文稿散列都必须校验。未来公共用户不能直接修改这些表。
- 新人物先不填尚未存在的“最高学历/职务”指针；先批准人物和经历，再通过新修订补指针。
- 关系、阶段、人物事件、文稿修订放在同一个必要批次中，避免只更新其中一处。辅助索引 `mentions/identity_checks/segment_people/kinship_links` 必须与批准后的内容一致；不能只填索引而绕过修订。
- 所有提交的引用也属于修订内容。引用变化要产生新版本。
- 一个批次是一个事务：任一字段、外键或版本冲突失败，整批回滚；重新读取后再提交，不能覆盖别人的编辑。

受信任本地维护者可登记真实操作人，以下为命令格式，需替换为实际人员，不能伪造“两人复核”：

```sh
python3 scripts/review.py add-operator --id 实际提交人ID --name 实际姓名 --role contributor
python3 scripts/review.py add-operator --id 实际审核人ID --name 实际姓名 --role reviewer
python3 scripts/review.py submit 本地批次.json --actor 实际提交人ID
python3 scripts/review.py queue
python3 scripts/review.py review 返回的请求ID --actor 实际审核人ID --decision approve --reason 实际复核理由
```

审核可选 approve / reject / needs_changes。提交者不得自审。修订恢复使用旧内容建立新请求，不删除历史。CLI 只是本地工具，不提供网站注册或登录。此次全库迁移为明确标注的 legacy_import，不假扮完成了上述人工双人审核。

### 审核清单

- 身份是同一人吗？称谓是否真实公开别名？是否有同名歧义？
- 证据到底支持哪一条内容？是原始材料还是转载？反证是否也保留？
- 是亲属、工作关系、私人关系，还是仅共同出场/被一起提及？
- 时间是实际发生时间还是报道/作品年份？区间是否有精度和终点状态？
- 评论、预测、传闻有没有误写成事实？事件有没有误改变人物状态？
- 是否出现无依据的评分、派系或照片？
- 视频时码是否越界？章节范围有没有被写成逐句校时？
- 投稿批准与事实核实是否保持分开？

## 3. 发布前必须通过的检查

```sh
python3 scripts/validate.py
python3 scripts/test_v9.py
python3 鲁社长/数据库/export.py
python3 鲁社长/关系图谱/build.py
python3 scripts/export_public.py
```

此外必须：备份；对比人数/稳定 ID/关系数；确认只变更预期条目；确认所有投影与当前修订相同；校验公开快照能在空库恢复；浏览器检查点击人物、搜索/分类、时间轴、来源回看以及桌面/手机布局。

新增资料先在副本运行上述步骤，通过后在主库重复同一可复现流程。入库日志使用修订、审核、audit_events；全库迁移另有 schema_migrations 与 change_log。迁移自动创建 SQLite backup，不覆盖已有备份。

公开快照只分发已发布的白名单数据。账号、草稿、审核意见、票据、凭据、原始版权字幕与未授权照片不能随仓库或网页发布。发布到远程站点时沿用原项目。

## 4. 历史资料的合规边界

旧资料可以作为 legacy_import 基线保留，但不得用“迁移成功”冒充“事实已核实”。每人必须有 review_results；缺少身份来源、未知日期、旧文稿不完整分别记录 finding。待核说法仍可按其来源与状态显示，不能升级为 supported。

后续复核应关闭对应问题记录并链接修订；已接受的未知日期不能为了提高完整率而猜填。人物职业、关系与来源的既有稳定 ID 必须延续。
