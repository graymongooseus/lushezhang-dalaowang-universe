# 头像目录

公开仓库仅包含 `manifest.json` 来源清单，不包含照片。缺图时网页自动显示姓名占位。

如自行取得合法使用许可，将图片保存到清单 `file` 字段对应路径，并核对身份、作者、授权链接和署名要求。不要用同名者或生成的脸代替真实人物。

运行 `build.py --without-photos` 可明确构建无照片版本。不要把仅供本地使用的照片及内嵌这些照片的 HTML 提交到仓库。
