# 贡献指南

先阅读 `AGENTS.md` 和 `工作流.md`。修正姓名或关系时，请提供原始来源及视频时间入口，说明是公开事实、主播叙述、推测还是转述；不要把同名者、字幕错字或同段出现自动合并成人物关系。

代码改动请运行 `python3 scripts/validate.py`，重新构建，并在浏览器检查相关交互、手机布局和控制台。所有功能必须可在缺失照片时使用。

数据从 SQLite 更新，再运行 `scripts/export_public.py` 导出当前公开快照。不要直接手改网页内嵌数据，不要提交私人凭据、备份、临时转写、原始音视频或授权不清的图片。界面呈现方式不改变底层证据状态。

新增依赖请说明必要性；优先保留标准库构建和纯静态部署。提交 PR 时描述问题、改动和验证结果。贡献者仅提交自己有权贡献的代码、文字和素材。
