# 鲁社长人物谱系 · 持续维护工作流

适用：用户新增 YouTube 视频、提供人物身份更正、要求补充事件或更新交互网页。用户最新要求优先于本文。

## 1. 接收与去重

提取视频 ID，忽略播放起点参数，分析整段视频。先查询 sources，避免重复导入。读取数据库版本、当前人物与关系数量，备份 SQLite 后再修改。

## 2. 阅读字幕和定位

优先用浏览器 `exportYouTubeTranscript()` 读取字幕；核对标题、上传者、发布日期、时长和简介里的录制时间。剪辑上传日期不等于原节目日期。字幕、音频与简介冲突时记录限制，优先核听具体片段。

逐段检查全部人名，优先完整收录中共人物、中国官员和商人；也检查节目中的其他具名人物。推荐视频、评论区、广告不算本视频提及。公司、泛称职位、网友账号不冒充实名人物。

本地保存结构化人物提及、时间位置和原创摘要；仅保留必要的短摘录。不因为链接分析任务而归档整份受版权保护的第三方字幕。用户自行提供或确认有权使用全文时，按其授权另存原稿。

### 2.1 先保存逐段 Markdown，再更新数据库（每期必做）

完整读完视频后，先写 `鲁社长/文稿分析/<视频ID>_人名与关系.md`，包括：视频元信息、文字关系总览、全部姓名核对表、逐段人物事件／轶事／评价／动机推测／反事实示例、匿名待确认项、矛盾和反证。每段保留起止秒数、跳转链接、人物、原意摘要及说法类型；包括家庭、求学、求职、金钱、人际接触等细节，不能只摘主线。正文用原创概述，不保存字幕错名，不将简介标签或评论区人物算入正文。

MD 完成后才执行人物和关系的数据库增量写入。将分析文本收录到 video_analyses，将片段写入 video_segments，关联人物写入 segment_people；后续网页及可下载 MD 均从 SQLite 导出。逐段原稿修订须同步数据库并写日志。发生日期与播放秒数严格分开，传闻／模拟对话不创建事实事件。

## 3. 每个人名都核对

按“完整姓名 + 机构/职务 + 地区 + 年代”匹配，优先政府、机构、企业、本人或官方频道资料，再用可信媒体交叉验证。常见姓名必须检查同名不同人；中英文转写、繁简体和同音字都需核对。

已确认：使用规范真实姓名和稳定人物 ID；真实的公开笔名/频道名和用户确认的绰号可关联。字幕错字不得进入人物名、别名、搜索词或新生成文稿。身份待定：只在待确认清单记录时间位置、称谓类型、缺失线索和后续需要核查的内容；不根据同音搜索结果猜一个人。

按2026-09-22用户更新，外国国家元首与仅观众身份账号不纳入活跃人物库；已删除项在 excluded_people 与变更日志保留审计，历史备份可追溯。媒体人物公开笔名可沿用稳定 ID。旧“其他”分组内有明确公众身份的演员、运动员等重新分类，不因旧分组删除。字幕错误不传播到姓名、别名或新文稿。

每期生成“姓名核对表”：规范姓名、核对状态、公开身份、来源、视频时间。核对表必须覆盖该期全部提及，包括最终进入待确认清单的项。

## 4. 提取人物信息与关系

人物：主分类必须且只能取“政治、军事、商业、媒体、学者、演员主持人、其他名人”之一；people.category 与 person_classifications 一致。圈层、政治立场、企业归属不混入主分类。人物字段、背景摘要、教育经历、最高学历、学位、任职履历（起止日期/地点/机构/职务）、最高已收录职务、信息更新时间。未查到的字段保留 NULL；最高已收录职务不是现任职务。

关系：A、B、方向、类型、起止时间、联系强度 1–5、亲疏/评价编码、评价范围、主张者、证据出处、具体时间位置、可信度和核实状态。无量化依据则不打分。公开评价与私人关系分开；只有共同出现不连线。亲属、共事、接任、提拔、合作、政治分歧分别记录。

派系：区分政治网络、正式政治组织、商业网络、节目协作圈、思想受众圈和学术网络。每项归属单独有来源、角色、时间、可信度。待定不构成共同阵营；不因“某某的人”一句话就写成已证实政治忠诚。

## 5. 时间与事件模型

- `start_date/end_date`：关系或任职的实际有效时间，保留来源精度（年/月/日）。终点未知不等于一直有效。
- `relationship_periods`：同一关系可有多段状态。状态变化新增一段，不覆盖旧历史；不能凭动画需要编造转折。
- `person_events`：任职、离任、调查、拘押、判刑、服刑、获释、软禁、出境、定居海外、出逃、逝世、遇害、执行死刑、竞选等，类型严格区分。
- `reported_at`：消息发布日；`recorded_at/updated_at`：本库收录/更新日。二者都不能替代实际发生日。
- `verification`：公开证据支持 / 主播说法待核实 / 预测 / 有争议。未经核实的事件不改变人物的事实状态。
- 已确认被调查也不代表已经定罪、服刑；正常移民不代表出逃；死亡不代表遇害。缺少实际事件依据时，只建立可支持该类型的字段，不造样例事件。
- 历史时间轴是“根据现有来源回看发生时间”，不是声称当年已经知道全部材料。默认全时期总览；历史时点只显示有年代依据的关系，未知时期独立开关且明显标识。
- 人物不因退出权力核心或死亡而从档案删除。时间轴保留姓名节点，用状态标记及关系显隐表达变化，不把无记录理解为当时不存在。

## 6. 头像

逐人搜索公开照片，核对来源页面和身份。优先本人/机构/政府/维基共享资源。下载图片并记录来源页、原图地址、许可说明、检索时间。网页使用本地或内嵌图片；不依赖容易失效的防盗链地址。仅有频道图标时注明“频道头像”，不声称是真人照片。

未确认身份、无可靠照片时使用姓名占位并明确标记。不得用另一位同名者、随机照片或 AI 生成脸替代真人。

## 7. 数据保存与网页生成

1. 对 `鲁社长/数据库/人物谱系.sqlite3` 使用 SQLite backup API 备份。
2. 在事务中迁移/增量更新，保留稳定 ID、来源和 change_log。必要时新增迁移脚本，不重跑历史初始化脚本。
3. 运行 `python3 鲁社长/数据库/export.py` 重新生成人物档案、CSV 和 JSON。
4. 运行 `python3 鲁社长/关系图谱/build.py` 生成 `index.html`。网页数据直接读取数据库，不手填第二份人物库。
5. 核对第2.1步已经提前保存本期 Markdown；网页输出逐段视频索引、人物档案时间链接及 Markdown 下载。不得等数据库修改完才补写初始分析。
6. 网页包含全部已收录人物、搜索/分类/圈层筛选、悬停背景、点击档案、关系证据、时间轴和事件轨迹；手机可点击查看。

## 8. 校验与交付

检查外键、重复姓名/ID、全部人物提及覆盖、日期先后、未来信息泄漏、未经核实事件不改变事实状态、同音姓名误配、图片加载与来源。检查历史时点前后：关系出现/结束、调查日期前后、逝世前后、未定年关系开关。核对没有为了让图好看而添边。

在浏览器验证桌面和手机：页面非空、控制台健康、所有姓名可检索、hover/点击、筛选、时间轴播放/拖动、来源跳转。新证据如果冲突，保留双方出处并明确提示。交付可直接打开的 HTML、更新后的数据库、每期分析和需要用户补充的少量身份问题；不宣称已完成尚未取得的核验。

## 9. 本地测试与运行环境

交付入口为根目录《打开人物谱系.html》，完整离线页面在 `鲁社长/关系图谱/index.html`。修改模板后必须重新运行 build.py；不得手改生成的 index.html。

需要 Python 3.10+；若系统 Python 不可用，可指定已经安装的 Python 可执行文件运行 export.py 和 build.py。

网页测试用例见 `鲁社长/关系图谱/使用说明.md`。本地预览可用 Python 的 http.server，绑定 127.0.0.1；该地址只用于本机测试。

## 10. 站点持续发布

站点源目录：`站点/`；现有项目 ID 位于 `站点/.openai/hosting.json`，后续必须复用，不再创建另一站点。build.py 会同时更新离线 HTML 和 `站点/dist/index.html`。发布只上传站点目录，不上传备份、数据库原文件和运行凭据。保持现有访问范围（当前为自定义分享），发布前读取站点访问配置；未经用户要求不扩大或缩减分享范围。

每次界面修改先备份模板与数据库，保存更新日志；检查正圆几何、点击居中动画、直接关联高亮、未关联节点透明度、时间轴与筛选组合，再更新既有站点。

## 11. 分类色环与圈层图

头像色环仅由七类主分类决定，在主图、档案和圈层图一致。圈层用独立方框及分支连线表达，允许一人多圈；待核实归属用虚线。没有归属记录不自动填入某个阵营。分类变动与删除必须同步 SQLite、导出、头像使用清单和网页。

## 12. 替代音频与新增名单

YouTube 无可读字幕时，可以核对同一主播、同题目的播客音频，但须记录版本、时长差异与可能删节。完整阅读替代音频后只采用已核实的原视频章节入口；`locator_precision=chapter`、`ingest_note` 向用户显示范围定位，不把播客秒数当视频逐句时间。没有可靠定位时不得编造。

每期首次入库将新增稳定 ID 保存到 `video_analyses.new_person_ids_json`，后续重建网页仍能区分当期新增与已有。网页可查看全部本期人物、仅本次新增、身份待确认名单并下载 MD。已经导入的视频不得重跑先行准备或导入脚本覆盖分析；修订先编辑 MD、同步 SQLite 并记录变更。

## 13. 三层亲属展开与占位

关系入库后，确属父母子女、兄弟姐妹、配偶或其他亲属类型的记录需同步 `kinship_links`；该表只索引原关系，不另造事实。亲近、友好、恋爱、后台或同姓不自动进入亲属索引。待核亲属沿用虚线及待核路径标记，多步路径含任何待核环节都不能显示为已证实。

选中人物后最多展开三步亲属路径；为包含用户指定的兄弟姐妹及姻亲，三层表示图路径，不是血缘上下三代。保持历史日期与未定年开关；空位表示没有收录信息，不是真实人物或推定亲属，不建立虚假人名节点。用户指定的长期入口记录在 `family_research`。后续摘要补充真实人物与有来源的关系后，网页自动替换空位。

## 14. 宇宙叙述模式

项目展示标题为“鲁社长大老王宇宙”。默认完整呈现已收录的节目说法，不因未独立证实而从浏览图中删去；界面统一注明主播叙述整理，推测与转述包含在内。核对标记可切换显示，原始证据保存在详情与数据库中，不将主张的核实状态改成公开事实。后续视频照常核实姓名和时间位置；没有具名身份、日期或关系的材料不补造节点或时间。

## 15. GitHub 开源分发

公开仓库：https://github.com/graymongooseus/lushezhang-dalaowang-universe 。运行所需数据从当前 SQLite 用 `scripts/export_public.py` 导出到 `data/snapshot.sql`；首次克隆用 `scripts/bootstrap.py` 恢复。不要直接编辑快照取代数据库。公开导出省略本地历史审计与已排除人物记录；备份、部署凭据、私人路径、临时转写和授权未核照片不上传。发布前执行 `scripts/validate.py` 并检查 `.gitignore`。
